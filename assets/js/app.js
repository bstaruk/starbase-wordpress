/**
 * Main theme JS
 *
 * Page-specific JS should be put in their own files (and added to the gulp build task so they are compiled into dist)
 */

(function ( $ ) {
	"use strict";

	// stuff goes here

})( jQuery );
